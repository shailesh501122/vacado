<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- HTTPS is preferred everywhere. Cleartext is only allowed for the
         production backend that currently runs on HTTP. Swap this for the
         real domain + HTTPS as soon as a TLS cert is provisioned. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">161.118.165.248</domain>
        <!-- Android emulator host loopback for local dev -->
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
